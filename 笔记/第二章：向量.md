# 第二章：向量

## 2.1 接口与实现

1 区分**抽象数据类型**和**数据结构**

* 抽象数据类型(ADT)

  * 数据类型：如int、char、float等

  * 抽象数据类型： Vector、List；不需要关系其细节，只需要使用其功能

* 2.1.2 数据结构(DS)

  

2 向量：

线性数组：A[i]的物理地址 = A + i\*s

向量是数组的抽象与泛华，由一组元素按**线性次序封装**

* 各元素与[0, n)内的秩一一对应
* 向量ADT接口：如size(), get(r), search(e)...



3 构造与析构

```cpp
public:
// 构造函数
   Vector ( int c = DEFAULT_CAPACITY, int s = 0, T v = 0 ) //容量为c、规模为s、所有元素初始为v
   { _elem = new T[_capacity = c]; for ( _size = 0; _size < s; _elem[_size++] = v ); } //s<=c
   Vector ( T const* A, Rank n ) { copyFrom ( A, 0, n ); } //数组整体复制
   Vector ( T const* A, Rank lo, Rank hi ) { copyFrom ( A, lo, hi ); } //区间
   Vector ( Vector<T> const& V ) { copyFrom ( V._elem, 0, V._size ); } //向量整体复制
   Vector ( Vector<T> const& V, Rank lo, Rank hi ) { copyFrom ( V._elem, lo, hi ); } //区间
// 析构函数
   ~Vector() { delete [] _elem; } //释放内部空间
```



4 copyFrom解析

```cpp
template <typename T> //元素类型
void Vector<T>::copyFrom ( T const* A, Rank lo, Rank hi ) { //以数组区间A[lo, hi)为蓝本复制向量
   _elem = new T[_capacity = 2 * ( hi - lo ) ]; _size = 0; //分配空间，规模清零
   while ( lo < hi ) //A[lo, hi)内的元素逐一
      _elem[_size++] = A[lo++]; //复制至_elem[0, hi - lo)
}
```

**分配空间的时候乘以2**是为了：预留了一些空间，接下来一段时间内不会因为扩容打断计算过程

注意hi和lo是**左闭右开**的！





## 2.2 可扩充向量

**1 静态空间管理：**容量_capacity固定，有以下缺点

* 上溢（overflow）：_elem\[\]，开辟的空间不够用
* 下溢（underflow）:空间利用率低下



**2 动态空间管理：**在**即将上溢**时，适当扩大内部数组的容量

扩容算法的实现

```cpp
template <typename T> void Vector<T>::expand() { //向量空间不足时扩容
   if ( _size < _capacity ) return; //尚未满员时，不必扩容
   if ( _capacity < DEFAULT_CAPACITY ) _capacity = DEFAULT_CAPACITY; //不低于最小容量
   T* oldElem = _elem;  _elem = new T[_capacity <<= 1]; //容量加倍
   for ( int i = 0; i < _size; i++ )
      _elem[i] = oldElem[i]; //复制原向量内容（T为基本类型，或已重载赋值操作符'='）
   /*DSA*/ //printf("\n_ELEM [%x]*%d/%d expanded and shift to [%x]*%d/%d\n", oldElem, _size, _capacity/2, _elem, _size, _capacity);
   delete [] oldElem; //释放原空间
}
```

得益于向量的**封装**，扩容后数据的物理地址有所改变，但是**不会出现野指针**

为什么**容量加倍**：





**3 递增式扩容：**

每次即将上溢的时候**追加固定大小容量**

最坏情况：

总体耗时：

每次扩容平均成本，时间复杂度是：$O(n)$



**4 加倍式扩容：**

向初始容量为1的满向量种，连续插入n=2^m >> 2个元素

平均时间成本$O(1)$



**5 分摊复杂度：**

平均分析 VS.  分摊分析



* 平均复杂度or期望复杂度：根据数据结构各操作出现的概率，将其对应的成本加权平均
  * 各种可能的操作，作为**独立事件**分别考察
* 分摊复杂度：对数据结构**连续地**实施**足够多次**操作，所需总体成本分摊到单次操作





## 2.3无序向量



### 单元素删除

调用区间删除操作

为什么不重复调用单元素删除操作去做区间删除呢？

因为这样做会。。。导致效率很低

### 查找

Entry

命中多个元素时，可返回秩最大者



### 唯一化

实例：网络搜索的局部结果去重操作，汇总为最终报告

deduplicate()



### 遍历





## 2.4有序向量

有序序列中，任意一对相邻的元素顺序

所以使用$disordered()$，判断序列中逆序对的个数。若为0，则说明序列有序

### 2.4.1  有序向量唯一化（低效版）

有序向量中，重复的元素必然相互紧邻构成一个区间。

所以每个区间保留单个元素即可。

